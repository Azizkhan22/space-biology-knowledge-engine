name: Ping API URL

on:
  schedule:
    - cron: "* * * * *" # ⏰ Runs every minute
  workflow_dispatch:       # ✅ Allows manual run from the Actions tab

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping your API endpoint
        run: |
          echo "Pinging Space Biology Knowledge Engine API..."
          curl -s -o /dev/null -w "Response code: %{http_code}\n" https://fb24d8a8-79f6-4df3-bcbb-0599559bba32-00-3361tn4h34gry.sisko.replit.dev/api/health
